---
title: Fleet CLI upgrade from nics-macbook-pro
type: session
status: open
tags: [session, housekeeping, upgrades]
created: 2026-08-19
updated: 2026-08-19
owner: nic
---

# Fleet CLI upgrade from nics-macbook-pro

Ran [[housekeeping]] from this Pro (hostname `nics-MacBook-Pro-3`, MagicDNS `nics-macbook-pro.taile8dc37.ts.net`) plus a second run on `mac-mini-3.taile8dc37.ts.net` after fixing a stdin bug that skipped every host after the scheduler.

## Measured (2026-08-19 ~00:40–00:48 UTC)

| Host | kimi | claude | cursor-agent | grok |
|---|---|---|---|---|
| **nics-macbook-pro** (this console) | 0.34.0 brew → **0.37.2** native (`~/.kimi-code/bin`); brew `kimi-code` 0.36.1 | native 2.1.222 → **2.1.227**; brew npm 2.1.220 → **2.1.227** | 2026.08.04 → **2026.08.11-e8db854** | already **1.0.5** via `~/.grok/bin` (`grok update`) |
| **mac-mini-3** | **0.37.2** | native **2.1.235** (PATH was leftover `/opt/homebrew/bin/claude` 2.1.31; now a symlink to native) | brew **2026.02.13-41ac335**; `cursor-agent update` **[unauthenticated]** | 1.0.4 → **1.0.5** (`brew upgrade grok-build`) |
| **magnus-mac-mini** | Kimi Code **0.37.2** installed (`~/.kimi-code/bin`); `kimi` on PATH is still kimi-cli | brew cask `claude-code` **2.1.29 → 2.1.227** (`claude update` only printed the brew hint) | keychain locked; still 2026.05.24 | still **missing** |
| nmba | not upgraded — Remote Login off, `alias` is null in `fleet.json` | | | |
| nics-mac-mini-2 | offline, Tailscale key expired | | | |
| prathams-macbook-air-1 | Tailscale ping: no reply | | | |

Dashboard republished 2026-08-19T00:48:11Z to `doconnect-sf:/var/www/mac-fleet/`.

## Bugs found

1. **`scripts/housekeeping.sh` local `bash -c "$REMOTE"` inherited the `while read` fleet.json stream.** First self-host (mini3 on the scheduled path) let brew/claude eat stdin; magnus and later hosts never ran. Explains why magnus was still on claude 2.1.29 while mini3 had been upgraded. Fix in working tree: `</dev/null`. Copied onto mini3's checkout; **not on `origin/main` — next `git pull` on mini3 will revert it.**
2. magnus Data volume was 349 Mi free. `brew cleanup -s` + deleting unused cursor-agent `2026.01.28-fd13201` → **2.6 Gi** (then 2.1 Gi after claude-code + kimi-code). See [[2026-08-14-magnus-disk-full]].
3. This Pro has **no** `~/.ssh/config` fleet aliases. `ssh nic@mac-mini-3` and `ssh nic@magnus-mac-mini` work (RSA `id_rsa`, short Tailscale names). MagicDNS FQDNs fail `Host key verification failed`. mini3 → this Pro still `Host key verification failed` / ssh-denied.

## Left open

- Push the stdin fix before the next scheduled housekeeping (`git pull` on mini3).
- `cursor-agent update` on mini3 and magnus (login / unlock keychain).
- nmba CLIs (must run on the Air).
- nmbp inbound SSH for the fleet key so the dashboard can see this laptop.
