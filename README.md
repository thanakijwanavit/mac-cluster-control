# mac-cluster-control

Control plane and Obsidian vault for Nic's Mac fleet on Tailscale.

Open **`Home.md`** (or this folder as an Obsidian vault). Agents: read **`AGENTS.md`** before changing anything, and write findings back into the vault — same completion bar as a `git push`.

```bash
# live Macs you can SSH to from nmba (2026-08-14)
ssh magnus    # nic@magnus-mac-mini.taile8dc37.ts.net
ssh mini3     # nic@mac-mini-3.taile8dc37.ts.net
scripts/probe-macs.sh
```
